
{
  "entities": {
    "user": {
      "title": "User",
      "description": "A user of the application.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "displayName": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "photoURL": { "type": "string", "format": "uri" },
        "isVerified": { "type": "boolean", "default": false },
        "verifiedAt": { "type": "string", "format": "date-time" },
        "verificationStatus": { "type": "string", "enum": ["none", "pending", "verified", "rejected"], "default": "none" },
        "isVerificationPaid": { "type": "boolean", "default": false },
        "idDocumentUrl": { "type": "string", "format": "uri" },
        "isBanned": { "type": "boolean", "default": false },
        "bio": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "fcmTokens": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["uid", "email", "displayName", "photoURL"]
    },
    "annonce": {
      "title": "Annonce",
      "description": "An advertisement or listing on the platform.",
      "type": "object",
      "properties": {
        "titre": { "type": "string" },
        "prix": { "type": "string" },
        "image": { "type": "string", "format": "uri" },
        "vendeurId": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "approved", "rejected", "shadowed"], "default": "pending" },
        "moderationReason": { "type": "string" },
        "description": { "type": "string" },
        "localisation": { "type": "string" },
        "whatsapp": { "type": "string" },
        "categorie": { "type": "string" },
        "etat": { "type": "string" },
        "manualReviewRequested": { "type": "boolean", "default": false },
        "views": { "type": "number", "default": 0 },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["titre", "prix", "image", "vendeurId"]
    },
    "favorite": {
      "title": "Favorite",
      "description": "A mapping of a user's favorite ad.",
      "type": "object",
      "properties": {
        "annonceId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["annonceId", "createdAt"]
    },
    "review": {
      "title": "Review",
      "description": "A review left for a seller.",
      "type": "object",
      "properties": {
        "rating": { "type": "number" },
        "comment": { "type": "string" },
        "reviewerId": { "type": "string" },
        "reviewerName": { "type": "string" },
        "reviewerPhotoURL": { "type": "string" },
        "sellerId": { "type": "string" },
        "annonceId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["rating", "comment", "reviewerId", "sellerId", "annonceId", "createdAt"]
    },
    "auditLog": {
      "title": "Audit Log",
      "description": "A record of an administrative or system action.",
      "type": "object",
      "properties": {
        "action": { "type": "string" },
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "targetId": { "type": "string" },
        "targetName": { "type": "string" },
        "details": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["action", "userId", "userName", "timestamp"]
    }
  },
  "auth": {
    "providers": ["email", "google.com", "apple.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/user" },
      "description": "Stores public user profile information."
    },
    "/users/{userId}/favorites/{annonceId}": {
      "schema": { "$ref": "#/entities/favorite" },
      "description": "Stores ads favorited by a user."
    },
    "/annonces/{annonceId}": {
      "schema": { "$ref": "#/entities/annonce" },
      "description": "Stores all listings/advertisements on the platform."
    },
    "/reviews/{reviewId}": {
      "schema": { "$ref": "#/entities/review" },
      "description": "Stores reviews for sellers."
    },
    "/auditLogs/{logId}": {
      "schema": { "$ref": "#/entities/auditLog" },
      "description": "Stores system and moderation activity logs."
    }
  }
}
