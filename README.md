# SuguMali E-commerce Platform

Welcome to SuguMali, your number one marketplace in Mali! This is a Next.js project bootstrapped with Firebase Studio.

## Getting Started

To get started, take a look at `src/app/page.tsx`.

The backend is powered by Firebase. You can find the data structure definition in `docs/backend.json` and Firestore security rules in `firestore.rules`.
