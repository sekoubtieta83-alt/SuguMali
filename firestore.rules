rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // RÈGLES DE DÉVELOPPEMENT : Accès complet autorisé à tout le monde.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}